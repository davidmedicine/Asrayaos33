// src/constants/commands.ts

/**
 * Defines the maximum number of recently used commands to store
 * in the command slice state and local storage.
 */
export const RECENT_COMMAND_LIMIT: number = 10; // You can adjust this number if needed

// Add other command-related constants here in the future if needed.