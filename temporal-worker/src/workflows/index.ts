/**
 * src/workflows/index.ts
 * ──────────────────────────────────────────
 * Barrel-file that re-exports every Workflow
 * implementation in this folder.  Add new
 * Workflows with one line each (or switch
 * to `export *` if you prefer).
 */

/* First-Flame ritual bootstrap */
export { seedFirstFlame } from './seedFirstFlame';

/*
 * 👇  Future workflows
 * export { anotherWorkflow }   from './anotherWorkflow';
 * export { nightlyCleanup  }   from './nightlyCleanup';
 */
